import type { FC } from "react";
const Agent: FC = () => {
    /* hooks区 */
    /* 计算值区 */
    /* (请求)API区 */
    /* uesEffect区 */
    /* handel区 */
    /* return区 */
    return <div>Agent</div>;
}

export default Agent;